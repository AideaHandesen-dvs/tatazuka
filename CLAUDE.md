# 佇か（たたずか）

伺かの精神的後継デスクトップマスコット。設計の全体像は `README.md`（育てていく設計メモ）を必ず参照。

## 絶対に守る設計判断

- **デスクトップ透過は諦めた**。表示はブラウザ端末（スマホ/タブレット/チャレンジタッチ）に逃がす。X11/Wayland 透過に戻る提案はしない。
- **プログレッシブ・エンハンスメント**：傾きセンサ・カメラが無くても佇かは出る。フル機能前提のコードを書かない。
- `DeviceOrientation` / `getUserMedia` は HTTPS（または localhost）必須。iOS の傾きはユーザー操作起点の許可が要る。

## 作業規律

- `protocol/` が server と client の契約。プロトコル変更は**必ず両側を同じセッションで**直す。片側だけのその場調整は禁止。
- client は server が静的配信する構成（オリジン一つ・HTTPS 終端一箇所）。
- 将来 systemd user サービス化するので、リポジトリ名 `tatazuka` と `server/` のパスは改名禁止。
- 表示クライアントは低性能端末対応のため軽く・枯れた技術で。ビルド不要（バンドラ無し）を目指す。
